// Update the like count display
$('.like-count').text('<%= @post.likes.count %>');

// Update the link/button text to "Unlike"
$('.like-button').text('Unlike');

// Add a class to the link/button for styling
$('.like-button').addClass('unliked');