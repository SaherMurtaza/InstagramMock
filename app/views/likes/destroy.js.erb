// Update the like count display
$('.like-count').text('<%= @post.likes.count %>');

// Update the link/button text to "Like"
$('.like-button').text('Like');

// Remove the class added for styling
$('.like-button').removeClass('unliked');